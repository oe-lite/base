# -*- mode:python; -*-
LICENSE = "BSD-3-Clause"

require pcre.inc

SRC_URI += "file://pcre-cross.patch"
SRC_URI += "file://fix-pcre-name-collision.patch"

LIBRARY_VERSION = "0"
LIBRARY_VERSION_${PN}-libpcre = "1"

DEFAULT_PREFERENCE = "-1"
