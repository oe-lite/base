LICENSE = "MIT"

require expat.inc

SRC_URI:>HOST_LIBC_mingw = " file://libtool-bindir.patch"
export sharedlibdir

SRC_URI += " file://expat-CVE-2015-1283.patch"

require conf/fetch/sourceforge.conf
SRC_URI = "${SOURCEFORGE_MIRROR}/expat/expat-${PV}.tar.gz"

LIBRARY_VERSION = "1"
